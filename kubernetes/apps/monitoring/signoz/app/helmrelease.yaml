---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: signoz
spec:
  interval: 30m
  chart:
    spec:
      chart: signoz
      version: 0.94.1
      sourceRef:
        kind: HelmRepository
        name: signoz
  values:
    clickhouse:
      persistence:
        existingClaim: signoz
