---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: flagsmith-postgres-owner
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: flagsmith-postgres-owner
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: flagsmith
        property: FLAGSMITH_DB_USER
    - secretKey: password
      remoteRef:
        key: flagsmith
        property: FLAGSMITH_DB_PASSWORD
